# ── Database ─────────────────────────────────────────────────────────────────
psycopg[binary]>=3.2
sqlalchemy>=2.0
python-dotenv>=1.0

# ── Analytics Engineering ────────────────────────────────────────────────────
dbt-core>=1.8
dbt-postgres>=1.8
# Pin newer mashumaro for Python 3.14 compatibility. dbt-core's own
# constraint (<3.15) is too tight — 3.21 works fine at runtime and is
# required for newer Python typing internals.
mashumaro[msgpack]>=3.21

# ── Data Pipeline ────────────────────────────────────────────────────────────
pandas>=2.2
pyarrow>=15.0
requests>=2.32
tqdm>=4.66

# ── Streamlit App ────────────────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.22
pydeck>=0.9

# ── LLM (Explain This Chart feature) ─────────────────────────────────────────
groq>=0.9

# ── Dev / Quality ────────────────────────────────────────────────────────────
pytest>=8.2
ruff>=0.6
